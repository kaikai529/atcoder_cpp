#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(val_name,start,end) for(ll val_name=(ll)start;val_name<end; val_name++)


int main(void){
    char a, b, c;
    cin >> a >> b >> c;
    string abc = string({a, b, c});
    // cout << atoi(a+b+c) + atoi(b+c+a)+ atoi(c+a+b);
    return EXIT_SUCCESS;
}